#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 * jack_bauer - entry point
 * Description: it checks if the parameter entered
 * is in lower case using prototype int _abs(int)
 * Return: Always 0 (Success)
 */
void jack_bauer(void) /* This is the function used */
{
_putchar(abs(c));
return (0);
}
